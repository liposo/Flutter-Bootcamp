import 'package:flutter/material.dart';

class IconHelper {
  Icon checkIcon() {
    return Icon(
      Icons.check,
      color: Colors.green,
    );
  }

  Icon closeIcon() {
    return Icon(
      Icons.close,
      color: Colors.red,
    );
  }
}
